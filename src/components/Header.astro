---
import { Icon } from "astro-icon";
import DropdownMenu from "./react/DropdownMenu";
import type { HomePage } from "../lib/content/model";

interface Props {
  menu: HomePage["menu"];
}

const { menu: navMenuItems } = Astro.props;

const languageMenuItems = [
  {
    text: "English",
    link: "/en-US",
  },
  {
    text: "Português (Brasil)",
    link: "/pt-BR",
  },
];
---

<header class="z-50 bg-zinc-100 border-b-2 border-zinc-900 py-3 sticky top-0">
  <nav class="container">
    <ul class="flex flex-row items-center justify-end gap-2">
      <DropdownMenu client:only as="li" items={languageMenuItems}><Icon size="24" class="text-zinc-900" name="ri:translate-2" /></DropdownMenu>
      <DropdownMenu client:only as="li" items={navMenuItems}><Icon size="24" class="text-zinc-900" name="ri:menu-fill" /></DropdownMenu>
    </ul>
  </nav>
</header>
